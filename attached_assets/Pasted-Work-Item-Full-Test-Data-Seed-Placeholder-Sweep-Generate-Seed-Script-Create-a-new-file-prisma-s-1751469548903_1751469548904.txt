Work Item: Full Test Data Seed + Placeholder Sweep

Generate Seed Script

Create a new file prisma/seed.ts or scripts/seed.ts (depending on your setup) that uses Drizzle (or knex/pg client) to insert:

Tenants

acme-corp (name: “Acme Corp”, logo: sample URL, owner profile)

beta-llc (name: “Beta LLC”, logo, owner profile)

Business Profiles

Full contact info, branding fields for each tenant.

Operating Hours

Mon–Sat 9:00–18:00, Sun closed.

Locations & Departments

Acme: “FOH Restaurant”, “Bar”, “Office”

Beta: “Back Office”, “Warehouse”

Job Roles

Acme: “Server”, “Bartender”, “Chef”, “Manager”

Beta: “Clerk”, “Supervisor”, “Driver”

Staff

5 staff per tenant, each assigned a role & location, with profile pics.

Shift Templates

Recurring template “Morning Service” (9:00–13:00 Mon–Fri).

“Evening Shift” (17:00–21:00 Tue–Sat).

Shifts

Generate 20 shifts per tenant over the past month—some open, some claimed, some assigned, some clocked in/out.

For each shift: random status (open/claimed/assigned/clocked-in/clocked-out), staff_id where relevant, clock-in/out timestamps.

Holidays & Sickness

3 holiday requests per tenant (past & future), mixed statuses (approved/declined/pending).

2 sick-day entries.

Run Seed

Add "seed": "ts-node prisma/seed.ts" to your package.json scripts.

Execute npm run seed (or yarn seed) to fully prime the database.

Placeholder Sweep

Globally search for “placeholder” or static values like “A LOCATION”, “Jane Doe”, hard-coded counts (shifts allocated: 16 (placeholder)) in your React code.

For each occurrence:

If it’s safe to remove, delete it.

If it indicates a feature not yet wired (e.g. a button with no onClick), wrap it in a modal that says “⚠️ TODO: implement [feature name]” and log it into a “Pending Tasks” queue.

Audit Report

After seeding & sweep, produce a JSON report listing:

All pages/components that still show placeholder text or have placeholder modals.

Endpoints that return empty arrays or 400/404 (i.e. still un-wired).

Save it to reports/placeholder-audit.json.

When you run this, you’ll end up with a realistic, multi-tenant dataset and a clear audit of anything still un-wired so you can hit your next checkpoint with full confidence. Let me know once the seed completes and share the audit report!