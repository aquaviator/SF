gent Shifts, our health check has uncovered five high-priority, blocking categories. Let’s fix them in this exact order, one at a time, with verification after each step:

1. **Test Infrastructure (Jest timeouts)**  
   a. In `jest.config.js`, set a global timeout:
   ```js
   export default {
     testEnvironment: 'jsdom',
     setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
     testTimeout: 30000,
     maxWorkers: 1,
     transformIgnorePatterns: [],
   }
b. Ensure src/setupTests.ts mocks globals (fetch, window.matchMedia, etc.) and imports @testing-library/jest-dom.
c. Run a single smoke test:

lua
Copy
Edit
npx jest --testPathPattern=ShiftsPage.test.tsx --silent
d. Confirm it completes in under 5s. Report pass/fail and adjust until it reliably completes.

TypeScript Errors
a. Run npm run check to list all errors.
b. Fix schema mismatches: add missing fields (assignedAt, createdAt) to Zod/Drizzle schemas and TS types; align apiRequest payload types.
c. Correct generic type conflicts (e.g. DataTable<T>).
d. Re-run npm run check and confirm zero errors.

ESLint Issues
a. Run:

arduino
Copy
Edit
npm run lint -- --fix
b. Manually address remaining errors (unused imports, missing React imports, etc.).
c. Verify:

arduino
Copy
Edit
npm run lint
reports 0 errors and 0 warnings.

Schema & CRUD Mismatches
a. Audit each page’s storage calls versus shared schema:

Shifts: date/time fields

Opportunities: method signature

Swap Requests, Assignments, HolidayRequests: endpoint paths & payload fields
b. Update Zod schemas or UI code so they agree.
c. Re-run TS and tests for each module to confirm fixes.

Build Process (Vite timeout)
a. In vite.config.ts, adjust build settings:

js
Copy
Edit
export default defineConfig({
  build: { target: 'es2018', minify: false },
  server: { hmr: { overlay: false } }
});
b. Run:

arduino
Copy
Edit
npm run build
and confirm it completes successfully.

Verification & Commit
After each step, re-run the relevant command to verify success. Once all five categories are green, run the full pipeline:

arduino
Copy
Edit
npm run lint && npm test && npm run build && npm run check
Confirm ✅ HEALTH CHECK PASS.

Finally, commit with:

scss
Copy
Edit
chore(ci): fix Jest timeouts, TS errors, ESLint issues, schema mismatches, and build config
and include a brief Deviation Report listing any config changes.

Please begin with Step 1, verify its success, and then report back before moving on to the next step.```









Ask ChatGPT
