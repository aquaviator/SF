Implement Dynamic “Holiday Entitlements” Tab in Owner Workforce Management

Context: The “Holidays → Holiday Entitlements” section in the Owner dashboard is currently a static placeholder. We need to turn it into a fully dynamic, mobile-friendly table powered by real data, with console logging for each key event, pre-rendered modals, and end-to-end tests—while leaving all other functionality untouched.

API Endpoints & Data Handling

GET /api/holiday-entitlements → returns array of { userId, entitlementDays, usedDays, pendingDays } for this business.

PUT /api/holiday-entitlements/:userId → updates entitlement (admins only).

GET /api/my-holiday-requests?status=pending → for pendingDays calculation (reuse existing endpoint).

GET /api/time-entries?type=holiday&status=approved → for usedDays calculation (if needed).

Console logs:

js
Copy
Edit
console.log('OWNER: fetching holiday entitlements…');
console.log('OWNER: entitlement data →', data);
UI Layout & Responsiveness

Replace static table with <table class="min-w-full overflow-x-auto">.

Columns: Staff Member, Entitlement, Used, Pending, Remaining.

Above table, add a Filter input to search by name:

html
Copy
Edit
<input data-cy="holiday-entitlement-filter" … @input="onFilterChange" />
On narrow screens, allow horizontal scroll (.overflow-x-auto) and wrap header & filter in .flex flex-col sm:flex-row.

Calculations & State

Compute remaining = entitlementDays – (usedDays + pendingDays).

Store full list in component state; filter in-memory on user input.

Console log filter actions:

js
Copy
Edit
console.log(`OWNER: filtering entitlements by "${query}" → ${filtered.length} results`);
Edit Entitlement Modal

Pre-render a hidden <HolidayEntitlementModal> in template.

When clicking the “Entitlement” cell, open modal (no runtime template injection behind UI).

Modal shows current entitlement, allows adjusting days.

Buttons: Save and Cancel.

Save → PUT /api/holiday-entitlements/${userId} with new entitlementDays.

Console log:

js
Copy
Edit
console.log(`OWNER: saving entitlement for ${userId} → ${newValue}`);
Error Handling & Isolation

Wrap all API calls in try/catch; on error console.error('OWNER: entitlement fetch failed', err).

Show inline error banner; don’t reload other dashboard areas.

Testing Requirements

Jest unit tests for the entitlement component’s fetchEntitlements(), computeRemaining(), and onFilterChange().

Mock the API (jest.mock('../lib/api')) and verify console logs are invoked.

Cypress E2E:

Login as owner → navigate to Workforce → Holidays → Entitlements.

Assert table loads real data stub.

Type in filter → see table rows reduce.

Click an entitlement cell → modal appears, change value, click Save → table updates.

Verify PUT request payload and toast notification.

Mobile-First & Console Logging Everywhere

Ensure the table and filter collapse gracefully below 640px.

Include console.log in every data-loading, filtering, modal-opening, and save step.

Pre-render all modals hidden via CSS so they never sit behind other elements.

Please implement the dynamic Holiday Entitlements tab with these specs—fetching live data, mobile-friendly layout, inline editing modals, exhaustive console logging, and full unit & E2E test coverage—while keeping all existing routes and owner dashboard functionality untouched unless a schema change is absolutely required.