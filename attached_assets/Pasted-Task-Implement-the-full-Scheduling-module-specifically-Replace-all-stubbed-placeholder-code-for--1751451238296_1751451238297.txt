Task:
Implement the full Scheduling module, specifically:

Replace all stubbed/placeholder code for shift templates and live operations with a fully functional template system and real-time clock-in/out logic.

Requirements:

Develop the complete template system (CRUD for shift templates, apply to schedule, edit, delete, assign).

Implement real-time clock-in/out functionality for staff users, with live updates to the database and UI.

Ensure role-based access for scheduling features.

All existing and new scheduling logic must be type-safe and fully integrated with the current API and state.

Testing & Regression:

Build or update a comprehensive test suite for all scheduling features:

Unit tests for all scheduling logic and utility functions

Integration tests for shift template CRUD and real-time clock-in/out (including database persistence and UI updates)

Regression tests to ensure no existing scheduling or dashboard functionality is broken

Accessibility and navigation tests for all related pages/components

Run the entire test suite, and paste the full output as test evidence before and after any code changes.

All tests must pass with zero errors or warnings before work is considered complete.

If any test fails at any point, fix the issue and rerun tests, providing updated evidence.

Reporting:

At each step (pre-dev, post-dev, post-test), clearly state:

What was changed/implemented

What was tested (with actual test output pasted)

If any regressions or issues were discovered

Only consider the task complete when all features are working, test coverage is complete, and evidence is provided.

Extra context for the Agent:

The Scheduling module is currently only partially implemented—templates and clock-in/out are stubbed.

All new code and tests must integrate cleanly with existing role-based navigation, dashboard, and data models.

Prioritize clear, maintainable code and test evidence at each step.

Deliver:
A fully working Scheduling module with real-time clock-in/out, full CRUD for templates, no regressions, and complete test evidence demonstrating everything works.