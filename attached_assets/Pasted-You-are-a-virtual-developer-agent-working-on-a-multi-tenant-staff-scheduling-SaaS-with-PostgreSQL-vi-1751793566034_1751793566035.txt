You are a virtual developer agent working on a multi-tenant staff-scheduling SaaS with PostgreSQL via Drizzle ORM, Node.js/Express back end, and a React + Tailwind front end, using Gmail SMTP for email.

**Task: Complete Full Onboarding Workflow**  
**Goal:** Fix registration so that it:  
1. Creates all required records (business profile, subscription, default locations/job roles, shift policies).  
2. Sends activation email.  
3. Hooks up the activation page in the router.  
4. Implements login flow post-activation.

**Instructions:**

1. **Email Service**  
   - In `src/utils/mailer.ts`, configure Nodemailer with `GOOGLE_DELEGATED_EMAIL` and `GOOGLE_APP_PASSWORD`.  
   - Export `transporter`.

2. **Registration API (`POST /api/register-business`)**  
   - After inserting `tenants` and `users` (with `is_active=false`, `activation_token`, `token_expires_at`), also:
     - Insert into `business_profiles` using the new tenant’s ID and provided business info.
     - Insert initial `subscriptions` row using chosen plan or default seat pricing.
     - Seed default `locations`, `job_roles`, and `shift_policies` for the tenant.
   - Send activation email:
     ```js
     await transporter.sendMail({
       from: process.env.GOOGLE_DELEGATED_EMAIL,
       to: owner.email,
       subject: 'Activate your ShiftFlo account',
       html: `<p>Click <a href="https://app.shiftflo.com/activate?token=${token}">here</a> to activate.</p>`
     });
     ```
   - Return `{ tenantId, ownerId, activationLink }`.

3. **Activate Page Routing**  
   - In `client/src/App.tsx` (or routes file), add:
     ```jsx
     <Route path="/activate" element={<StaffActivation />} />
     ```
   - Ensure `/pages/staff-activation.tsx` is imported and used.

4. **Activation Endpoints**  
   - Verify `GET /api/auth/verify-token` (token validity).  
   - Complete `POST /api/auth/activate` (set password, mark `is_active=true`, clear token).

5. **Login Flow**  
   - Add `POST /api/auth/login` to verify credentials, issue JWT in HttpOnly cookie.  
   - Add `GET /api/auth/me` returning current user.  
   - Front-end: create `/pages/Login.jsx`, call `/api/auth/login`, store state.  
   - Protect private routes with an `AuthGuard` that fetches `/api/auth/me` and redirects to `/login` if unauthenticated.

**Output:**  
Reply **only** with the code diffs or new file contents for:
- `src/utils/mailer.ts`
- Updated `src/routes/register-business.ts`
- New or updated seed logic for `business_profiles`, `subscriptions`, `locations`, `job_roles`, `shift_policies`
- Activation email send code
- `client/src/App.tsx` routing diff
- `/api/auth/login` and `/api/auth/me` handlers
- `client/src/pages/Login.jsx` and integration of `AuthGuard`
No extra explanation.
