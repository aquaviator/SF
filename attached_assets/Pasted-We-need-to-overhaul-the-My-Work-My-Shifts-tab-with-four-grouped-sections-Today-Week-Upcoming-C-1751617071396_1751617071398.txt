We need to overhaul the My Work > My Shifts tab with four grouped sections (Today, Week, Upcoming, Completed) and ultra-lean shift cards that open a detail modal. Upcoming shifts must display escalation countdowns and available actions. Please implement the following:

1. Grouping & Data Loading
Fetch once via staffApi.getMyShifts(userId) in loadMyShifts(), then split into four arrays:

Today (date = today)

This Week (Mon–Sun)

Upcoming (after this week)

Completed (before today)

Log each bucket:

js
Copy
Edit
console.log('MyShifts-Today:', todayShifts);
console.log('MyShifts-Week:', weekShifts);
console.log('MyShifts-Upcoming (page', page, '):', upcomingShifts);
console.log('MyShifts-Completed (page', page, '):', completedShifts);
2. Card UI
Display per card:

Badge: status (completed, cancelled, clocked_out, etc.)

Role

Date • Time (e.g. 2025-07-10 • 09:00–17:00)

Location

Omit long descriptions—keep cards minimal.

Actions on Upcoming: show “Honor Shift” or “Swap/Cancel” buttons with a live countdown timer until the policy deadline. Example:

pgsql
Copy
Edit
🔔Swap until 2h 15m before start
Clicking any card opens a pre-mounted <ShiftDetailModal> showing full info (description, notes, policy deadlines, escalation history).

3. Pagination & Modals
Upcoming & Completed: paginate at 10 items; “Load More” button to fetch next page.

Modals must be mounted in DOM up front (not injected on click), toggled visible with proper aria-hidden and focus trapping.

Console-log every modal open/close:

js
Copy
Edit
console.log('Open ShiftDetailModal:', shiftId);
console.log('Close ShiftDetailModal');
4. Escalation Status
For Upcoming shifts nearing policy deadlines (e.g. < cancellation deadline), show an “⚠️ Escalates to Manager in Xh Ym” message.

Style: red text with icon if within escalation window.

5. Accessibility & Touch
Ensure 44×44 px minimum touch targets.

Keyboard-navigable cards and “Load More”/modal controls with proper ARIA roles/labels.

6. Tests
Update Jest unit tests for grouping logic, pagination, countdown display, and modal toggling.

Add a Cypress E2E that:

Logs in as staff, navigates to My Shifts

Verifies each section’s header count

Opens a shift card → checks modal content

Confirms countdown and action buttons appear correctly

Deliverables:

Refactored StaffDashboardPage (or equivalent) with new subcomponents: MyShiftsToday, MyShiftsWeek, MyShiftsUpcoming, MyShiftsCompleted, and ShiftDetailModal.

All console logs as specified.

Updated tests.

Please implement this in the next PR so the staff “My Shifts” tab is clean, actionable, and policy-aware.