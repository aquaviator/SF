Objective:
Implement the new “My Work” staff hub as a tabbed interface, consolidating all staff work functions (Schedule, Assignments, Time Tracking, Requests) as described below.
QA and test rigor must be maintained at every step, system-wide.

Requirements:
UI/UX:

Replace the “My Shifts” page with a new “My Work” page accessible from staff navigation.

Use a tabbed layout with 4 tabs: Schedule, Assignments, Time Tracking, Requests.

Each tab must present the following:

Schedule: Calendar view of assigned shifts, with details/status indicators.

Assignments: List of work instructions for each shift, fully integrated with backend (read-only).

Time Tracking: Live clock-in/out, break management, full time history.

Requests: Swap and holiday requests, with outgoing/incoming tracking.

Integration:

Merge the current Assignments, Shifts, Time Tracking, and Requests functionality into this unified component.

All actions must update and read from the live API/backend.

Remove any duplicate “My Shifts” or “Assignments” pages/components.

Accessibility & Responsiveness:

Tabbed interface must be accessible (keyboard, screen reader ARIA roles, etc).

Fully responsive on mobile and desktop.

Code Quality:

No stubbed data—live data only.

Refactor or delete legacy pages/components.

Use type-safe API integration throughout.

Testing Requirements:
Development and QA workflow must always include the following for every feature:

Do the task

Write the tests

Run the tests

Report the test evidence/output

Run a full regression pack

Explicitly confirm zero regressions or list new issues

Specific Tests To Implement and Report:
Rendering of each tab and correct navigation between tabs.

Each tab loads the correct data and UI for the logged-in staff user.

All actions (clock-in/out, request submit, etc) are fully tested, with both success and failure paths.

API integration tests for all “My Work” backend endpoints.

Accessibility tests for tab navigation.

Regression tests for main dashboard, staff nav, and all pages affected by the refactor.

Report all test runs and any failed test cases with clear error output.

Deliverables:
New My Work hub with all four tabs, fully functional for staff.

All tests passing (show test log/output).

No regressions (explicit report).

Removed/disabled legacy “My Shifts” and “Assignments” pages.

One-line summary of what changed and what evidence you’re providing.

Do the work, build/run the tests, provide evidence, confirm zero regressions. If any test fails, report details and do not consider work complete until resolved.