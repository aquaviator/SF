Agent Shifts, our health check has uncovered five high-priority, blocking categories. Let’s fix them in this exact order, one at a time, with verification after each step:

---

1. **Test Infrastructure (Jest timeouts)**  
   a. In `jest.config.js`, set a global timeout:  
      ```js
      export default {
        testEnvironment: 'jsdom',
        setupFilesAfterEnv: ['<rootDir>/src/setupTests.ts'],
        testTimeout: 30000,          // 30s per test
        maxWorkers: 1,               // avoid parallel hangups
        transformIgnorePatterns: [], // ensure React components compile
      };
      ```  
   b. Ensure `src/setupTests.ts` mocks any globals (fetch, window.matchMedia, etc.) and imports `@testing-library/jest-dom`.  
   c. Run a single smoke test:  
      ```
      npx jest --testPathPattern=ShiftsPage.test.tsx --silent
      ```  
   d. Confirm it completes in under 5s.  
   e. Report pass/fail and adjust until that smoke test reliably completes.

2. **TypeScript Errors**  
   a. Run `npm run check` to list all 47 errors.  
   b. In priority order, fix schema mismatches:  
      - Add missing fields (`assignedAt`, `createdAt`) to your Zod/Drizzle schemas and TypeScript types.  
      - Align `apiRequest` payload types with your endpoint definitions.  
   c. Correct any generic type conflicts (e.g. DataTable<T> column definitions).  
   d. Re-run `npm run check` and confirm 0 errors.

3. **ESLint Issues**  
   a. Run `npm run lint -- --fix` to auto‐fix trivial issues.  
   b. Manually address the remaining (~20) errors: unused imports, missing React imports in JSX files, incorrect type annotations.  
   c. Verify `npm run lint` reports 0 errors and 0 warnings.

4. **Schema & CRUD Mismatches**  
   a. Audit each page’s storage calls vs. your shared schema:  
      - Shifts: ensure date/time fields match schema.  
      - Opportunities: correct method signature.  
      - Swap Requests, Assignments, HolidayRequests: ensure endpoint paths and payload fields align exactly.  
   b. For each mismatch, update either the Zod schema or the UI code so they agree.  
   c. Re-run TypeScript and tests for each module to confirm the fix.

5. **Build Process (Vite timeout)**  
   a. In `vite.config.ts`, increase the build timeout or disable minification temporarily:  
      ```js
      export default defineConfig({
        build: {
          target: 'es2018',
          minify: false,
          rollupOptions: { /* if needed */ }
        },
        server: { hmr: { overlay: false } }
      });
      ```  
   b. Run `npm run build` and confirm it completes successfully within the environment’s limit.

---

### Verification & Commit

After each step above:

- Re-run the **partial** command to verify that specific area is now green (e.g. just `npm test` after fixing Jest config).  
- Only once all five categories are green, run the **full pipeline**:
