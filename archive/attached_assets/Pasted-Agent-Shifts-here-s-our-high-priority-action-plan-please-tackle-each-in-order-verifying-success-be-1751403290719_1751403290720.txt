Agent Shifts, here’s our high-priority action plan—please tackle each in order, verifying success before moving on:

1. **Finish Test Conversions**  
   - Convert the remaining 12 test files from Jest to Vitest syntax (imports, `vi.fn`, mocks, etc.).  
   - Run:  
     ```
     npx tsc --noEmit
     npx vitest run --config vitest.config.ts
     ```  
   - Confirm **0 TS errors** and **100% passing**.

2. **Optimize Vite Build**  
   - In `vite.config.ts`, externalize or stub heavy libs:
     ```js
     optimizeDeps: { exclude: ['lucide-react','recharts','d3','lodash'] },
     build: {
       rollupOptions: {
         external: ['lucide-react','recharts','d3','lodash']
       }
     }
     ```  
   - Re-run:
     ```
     npm run build
     ```  
   - Confirm build completes under **60s**.

3. **Add Linting Script**  
   - Install ESLint and configure a `lint` script in `package.json`:
     ```jsonc
     "scripts": {
       "lint": "eslint client/src --ext .ts,.tsx"
     }
     ```  
   - Run:
     ```
     npm run lint
     ```  
   - Fix any errors so that it reports **0 errors, 0 warnings**.

4. **Implement Pre-commit Hooks**  
   - Set up Husky + lint-staged:
     ```bash
     npx husky install
     npx husky add .husky/pre-commit "npx lint-staged"
     ```  
   - In `package.json` add:
     ```jsonc
     "lint-staged": {
       "client/src/**/*.{ts,tsx}": ["eslint --fix", "vitest run --bail --runInBand"]
     }
     ```  
   - Verify committing code runs lint and tests, blocking on failures.

5. **Fix Missing Opportunity Apply Route**  
   - In your shifts/opportunities router, add the POST `/api/opportunities/:id/apply` endpoint.  
   - Test with:
     ```
     curl -X POST -H "tenantId: acme-corp" http://localhost:5000/api/opportunities/123/apply
     ```  
   - Confirm HTTP 200 and JSON response.

6. **Full Verification**  
   Run:
npm run check
npm run build
npx vitest run --config vitest.config.ts --coverage
npm run lint
Report back with “✅ READY FOR DEPLOYMENT” if all pass.

Let’s get these core blockers resolved so we can move into feature development and deployment!```
